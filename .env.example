# =============================================================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (Safe to expose - prefixed with VITE_)
# =============================================================================

# Supabase Configuration (auto-populated by Lovable Cloud)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
VITE_SUPABASE_PROJECT_ID=your_project_id

# Application
VITE_APP_NAME=YieldPilot

# Monitoring (optional)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Analytics (optional)
VITE_POSTHOG_KEY=phc_your_posthog_project_key
VITE_POSTHOG_HOST=https://eu.posthog.com

# Mapbox (Public token - safe for client side)
# Get your token from: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_API_KEY=pk.your_mapbox_public_token

# Stripe Publishable Key (safe for client side)
# Get your key from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# =============================================================================
# SERVER-SIDE SECRETS (Configure in Lovable Cloud/Supabase Secrets)
# =============================================================================
# These should NEVER have VITE_ prefix and should be configured as secrets:
# 
# - SUPABASE_SERVICE_ROLE_KEY (for admin operations in edge functions)
# - STRIPE_SECRET_KEY (for payment processing in edge functions)
# - APIFY_API_KEY (for web scraping in edge functions)
# - RESEND_API_KEY (for email sending in edge functions)
#
# Configure these via: Lovable Cloud → Backend → Secrets
# They are already configured in your project's Supabase secrets.
# =============================================================================
